package phase3;

// Base class for all statement nodes in the AST.
// Statements are actions that do not necessarily evaluate to a value (e.g., if, while, print, assignments).
public abstract class StatementNode extends ASTNode {
}
