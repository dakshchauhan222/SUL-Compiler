package phase3;

// Base class for all expression nodes in the AST.
// Expressions are components that evaluate to a value (e.g., numbers, identifiers, binary operations).
public abstract class ExpressionNode extends ASTNode {
}
